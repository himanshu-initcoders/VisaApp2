# 2026-08-18 - Daily Note

## 🎯 Today's Focus
- [x] Diagnose why the landing page was not using live country data
- [x] Align public landing queries with the current `countries` and `entry_processes` schema
- [ ] Unify public visa detail routing and data source

---

## ✅ Completed
- [x] Removed the landing page's hardcoded fallback country/process data
- [x] Updated public DB queries to use valid `schema-extended` relations and fields
- [x] Mapped `process_tiers` data into landing-page friendly price and ETA values
- [x] Prevented duplicate cards between featured destinations and the remaining grid
- [x] Disabled the broken public card link until a matching public detail route exists

---

## 💡 Ideas & Notes
- The public site is currently split between two content models: normalized `countries/entry_processes/process_tiers` and JSON-based `visa_listings`.
- The landing page should continue using the normalized country/process model, but the public detail page needs a matching route and query strategy.

---

## 🐛 Bugs Found
- `lib/db/queries/public.ts` referenced non-existent fields like `processTiers.order` and `entryProcesses.activeForBooking`.
- The landing page used dummy data because the live query path was crashing.
- Process cards were pointing to a non-existent public route shape.

---

## 🚧 Blockers
- `npm run build` still fails for an unrelated NextAuth/admin upload route:
- `app/api/admin/upload/presign/route.ts` imports `getServerSession` from `next-auth` and `authOptions` from `@/lib/auth`, but those exports do not exist in the current auth setup.

---

## 📚 Learnings
- The current homepage can be driven directly from `countries` with nested `entryProcesses` and `processTiers` relations.
- `process_tiers` ordering should be based on existing fields like `tierName` unless a dedicated display-order column is added.

---

## 🔗 Links & References
- `app/page.tsx`
- `lib/db/queries/public.ts`
- `lib/db/schema-extended.ts`

---

**Tags**: #daily-note
